/**
 * loads and decorates the cards-action block
 * @param {Element} block The cards-action block element
 */
// eslint-disable-next-line no-unused-vars
export default async function decorate(block) {
  // Pure CSS block — no JS decoration needed
}
